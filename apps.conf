# App registry — add new apps/packages here.
# Format: name|dir|precommit_cmd
#   name          display label used in pre-commit output
#   dir           path relative to repo root
#   precommit_cmd command to run inside that dir
frontend|apps/frontend|sh pre-commit
account-engine|apps/account-engine|sh pre-commit
alpha-etl|apps/alpha-etl|sh pre-commit
analytics-engine|apps/analytics-engine|uv run pre-commit run --all-files
