/**
 * Native connects straight to Privy (see `apps/app/src/providers/WalletProvider.tsx`)
 * — there is no wallet-or-Privy choice to render, so this host mounts nothing.
 */
export function ConnectSheetHost() {
  return null;
}
