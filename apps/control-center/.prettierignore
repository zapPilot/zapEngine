# `format`/`format:check` run `prettier .` from this directory, and prettier
# resolves .gitignore relative to its CWD — the repo-root .gitignore is never
# consulted. Without this file a local build makes format:check fail on dist/.
dist
coverage
.turbo
.jscpd

# Captured vendor markup. These fixtures exist to be byte-for-byte what the
# provider actually served, so reformatting them would quietly weaken the test
# that pins a parser against a page nobody here controls.
src/server/services/fly-billing/__fixtures__
