# prettier resolves .gitignore from its own CWD, so `prettier .` here
# needs this list — the repo-root .gitignore never applies.
dist
coverage
.turbo
.jscpd
*.tsbuildinfo

# Generated byte-for-byte from .agents/skills/social-headline/HEADLINE.md by
# `pnpm lint headline-policy`. Formatting it here rewrites the copy but not the
# canonical file, which puts the drift gate and the formatter in a loop.
prompts/social/headline.md
