// @zapengine/app-core — shared non-component application core (data services,
// React Query hooks, providers, wallet execution, domain helpers) consumed by
// both the web frontend and the desktop app.
//
// The public surface is exposed via subpath exports (e.g.
// `@zapengine/app-core/services`, `/hooks`, `/providers`, `/lib/http`); the
// root entry is intentionally empty.
export {};
