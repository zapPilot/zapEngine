name: zapengine_tokens
description: Shared ZapEngine design tokens for Flutter apps.
publish_to: 'none'
version: 0.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
