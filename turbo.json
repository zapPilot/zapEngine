{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".nvmrc"],
  "globalEnv": ["NODE_ENV", "CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "inputs": ["src/**", "package.json", "tsconfig.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "inputs": ["src/**", "*.config.*", "eslint.config.*"],
      "outputs": []
    },
    "type-check": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig.json"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tests/**", "test/**"],
      "outputs": []
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tests/**", "test/**"],
      "outputs": ["coverage/**"]
    },
    "format": {
      "outputs": [],
      "cache": false
    },
    "format:check": {
      "inputs": ["src/**", "tests/**", "*.config.*", ".prettierrc*", ".prettierignore", "pyproject.toml"],
      "outputs": []
    },
    "deadcode": {
      "inputs": ["src/**", "tests/**", "knip.json", "knip.production.json", "vulture_whitelist.py"],
      "outputs": []
    },
    "dup:check": {
      "inputs": ["src/**", ".jscpd.json"],
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
